This repository does not accept pull requests.

Method semantics live upstream in `mithran-hq/aegis-method` and arrive here
by distillation — see `CONTRIBUTING.md`. The license is MIT: fork it, paste
it, adapt it.
