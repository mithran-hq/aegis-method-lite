# Security

This repository ships prose and JSON contracts. There is no executable code,
no network call, and no install step beyond pasting text into your agent's
context — so the main risk is that text itself. Read the payload before you
hand it to an agent with tool access, as you would any instruction.

Report a concern through GitHub's private vulnerability reporting on this
repository.
